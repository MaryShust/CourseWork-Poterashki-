package com.maxim.poteryashki.gateway.service

import org.springframework.stereotype.Service
import java.util.UUID

@Service
class UserAuthService {

    fun getUserIdByToken(token: String): UUID? {

    }
}